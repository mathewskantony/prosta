import { SelectedDatesPipe } from './selected-dates.pipe';

describe('SelectedDatesPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedDatesPipe();
    expect(pipe).toBeTruthy();
  });
});
